import 'package:flutter/material.dart';
class RecentTabView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Recent tab'),
      ),
    );
  }
}
